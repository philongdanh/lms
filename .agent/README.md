# Agent Configuration

This directory contains configuration, skills, and workflows that define the
behavior of the AI agent (Antigravity).

## Structure

- **skills/**: Instructions and capabilities the agent can use. Each
  subdirectory represents a skill (e.g., `review`, `ssot`).
- **workflows/**: Defined procedures for complex tasks.
